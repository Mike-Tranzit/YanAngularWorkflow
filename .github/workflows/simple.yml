name: Shell Commands


on: [pssh]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name: multyline script
        run: |
          node -v
          npm -v
